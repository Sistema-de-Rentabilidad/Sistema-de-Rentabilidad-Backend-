name: Sistema Rentabilidad
version: 1.0.0
schema: v1

mcpServers:
  - name: filesystem
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-filesystem"
      - .

context:
  - provider: codebase
  - provider: repo-map
